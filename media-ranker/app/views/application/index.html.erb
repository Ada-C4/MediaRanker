<div>
  <h1>Top Movies</h1>
  <% @movies.each do |movie| %>
    <h2><%= link_to movie.title, movie_path(movie.id) %><small> Ranked: <%= movie.rank %></small>
  <% end %></h2>
  <%= link_to 'View More Movies', movies_path%>
</div>

<div>
  <h1>Top Books</h1>
  <% @books.each do |book| %>
    <h2><%= link_to book.title, book_path(book.id) %><small> Ranked: <%= book.rank %></small>
  <% end %></h2>
  <%= link_to 'View More Books', books_path%>
</div>

<div>
  <h1>Top Albums</h1>
  <% @albums.each do |album| %>
    <h2><%= link_to album.title, album_path(album.id) %><small> Ranked: <%= album.rank %></small>
  <% end %></h2>
  <%= link_to 'View More Albums', albums_path%>
</div>

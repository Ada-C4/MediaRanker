<div class="row">
  <div class="col-lg-12">
    <h1><%= @movie.title %>
        <small>Directed by: <%= @movie.director %></small>
    </h1>
    <h4>Ranked:<%= @movie.ranking %></h4>
    <p><%= @movie.description %></p>
    <div>
      <%= button_to 'Upvote', movie_upvote_path(@movie.id), method: :post %>
    </div>
    <div>
      # CRUD links for edit, delete, plus more
    </div>
  </div>
</div>

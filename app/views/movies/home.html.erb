<h1> Top Movies </h1>
<ul>
  <% @movies.each do |movie| %>
    <li><%= link_to "#{movie.name}", movie_path(movie.id) %></li>
    <li><%= movie.rank %></li>
    <% end %>
</ul>
<%= link_to "View More Movies", movies_path %>

<h1> Top Books </h1>
<ul>
  <% @books.each do |book| %>
    <li><%= link_to "#{book.name}", book_path(book.id) %></li>
    <li><%= book.rank %></li>
    <% end %>
</ul>
<%= link_to "View More Books", books_path %>

<h1> Top Albums </h1>
<ul>
  <% @albums.each do |album| %>
    <li><%= link_to "#{album.name}", album_path(album.id) %></li>
    <li><%= album.rank %></li>
    <% end %>
</ul>
<%= link_to "View More Albums", albums_path %>

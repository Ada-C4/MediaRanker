<table class="table table-hover">
  <thead>
  <tr>
    <th>Ranking</th>
    <th>Name</th>
    <th>Upvote</th>
  </tr>
</thead>
  <tbody>
    <% @model.each do |model| %>
      <tr>
        <td> <%= "placeholder ranking method" %></td>
        <td> <%= link_to model.title, model %></td>
        <td> <%="placeholder for upvote button" %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= link_to "View All Media", root_path, class:"btn btn-default", role:"button" %>
<%= link_to @add_button, @path, class:"btn btn-default", role:"button" %>

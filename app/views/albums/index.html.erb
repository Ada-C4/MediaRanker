<%= render partial:"index_page", locals: { items: @albums} %>


<!-- <table>
  <tr>
    <th>Ranking</th>
    <th>Name</th>
    <th>Upvote</th>
  </tr>
  <% @albums.each do |album| %>
  <tr>
    <td>
      Ranked: <%= album.ranking %>
    </td>
    <td>
      <%= link_to album.name, album_path(id: album.id) %>
    </td>
    <td>
      <%= button_to "Upvote", album_path(id: album.id) %>
    </td>
  </tr>
  <% end %>
</table>

<%= link_to "View All Media", "/" %>
<%= link_to "Add an Album", new_album_path %> -->

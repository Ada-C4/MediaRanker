<%= render partial:"index_page", locals: { items: @books} %>

<!-- <table>
  <tr>
    <th>Ranking</th>
    <th>Name</th>
    <th>Upvote</th>
  </tr>
  <% @books.each do |book| %>
  <tr>
    <td>
      Ranked: <%= book.ranking %>
    </td>
    <td>
      <%= link_to book.name, book_path(id: book.id) %>
    </td>
    <td>
      <%= button_to "Upvote", book_path(id: book.id) %>
    </td>
  </tr>
  <% end %>
</table>

<%= link_to "View All Media", "/" %>
<%= link_to "Add a Book", new_book_path %> -->
